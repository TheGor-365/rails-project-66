.container.py-5
  h1.mb-4 = t('views_i18n.repositories.new.title')

  - if flash[:alert].present?
    .alert.alert-danger = flash[:alert]

  = form_with model: @repository, url: repositories_path, local: true, data: { turbo: false } do |form|
    - if @repository.errors.any?
      .alert.alert-danger
        ul.mb-0
          - @repository.errors.full_messages.each do |message|
            li = message

    .mb-3
      - repository_options = @github_repositories.map { |repo| [repo.full_name, repo.id] }
      = form.label :github_id, t('views_i18n.repositories.new.github_label'), class: 'form-label'
      = form.select :github_id,
                    options_for_select(repository_options, @repository.github_id),
                    { include_blank: true },
                    class: 'form-select'

    = form.submit t('views_i18n.repositories.new.submit'), class: 'btn btn-primary'
