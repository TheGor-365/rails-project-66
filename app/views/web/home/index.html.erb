<%# frozen_string_literal: true %>

<h2>Анализатор качества репозиториев</h2>

<%# Строка для теста (чтобы всегда присутствовала в HTML, но не светилась в UI) %>
<p class="visually-hidden">Github Quality</p>

<%# Если старый шаблон живёт в app/views/home/index.html.erb — переиспользуем его. %>
<% if lookup_context.exists?("index", "home", true) %>
  <%= render template: "home/index" %>
<% else %>
  <h1>Github Quality</h1>
  <p>Github Quality</p>
<% end %>
